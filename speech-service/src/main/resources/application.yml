server:
  port: 8003

spring:
  servlet:
    multipart:
      max-file-size: 10MB

  application:
    name: speech-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7777/eureka