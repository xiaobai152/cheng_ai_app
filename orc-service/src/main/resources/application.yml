server:
  port: 8001

spring:
  servlet:
    multipart:
      max-file-size: 10MB

  application:
    name: orc-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7777/eureka